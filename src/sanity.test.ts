import { describe, it, expect } from 'vitest'

// Placeholder — proves Vitest runs. Delete once real finance tests exist.
describe('sanity', () => {
  it('arithmetic still works', () => {
    expect(1 + 1).toBe(2)
  })
})
